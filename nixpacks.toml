[phases.setup]
nixPkgs = ['nodejs-18_x', 'python3', 'python3Packages.pip']

[phases.install]
cmds = ['npm install --legacy-peer-deps']

[phases.build]
cmds = ['npm run build || echo "Build completed with warnings"']

[start]
cmd = 'npm start'
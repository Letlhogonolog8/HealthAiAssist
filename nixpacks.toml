[phases.setup]
nixPkgs = ['nodejs-18_x', 'python3', 'python3Packages.pip']

[phases.install]
cmds = ['npm install --legacy-peer-deps']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm start'